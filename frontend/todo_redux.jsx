import React from "react";
import ReactDom from "react-dom";

document.addEventListener('DOMContentLoaded', () =>{
  const roots = document.getElementById('root');
  ReactDom.render(<h1>TODOs APP</h1>, roots);
});
